use grid;
use grid::Grid;
use tile::Tile;

/*
pub fn draw_level(map: &Grid<Tile>) {
    for col in 0..grid::HEIGHT {
        mv(col as i32, 0);
        for row in 0..grid::WIDTH {
            addch(map[(row, col)].sprite().character as chtype);
        }
    }
}
*/
